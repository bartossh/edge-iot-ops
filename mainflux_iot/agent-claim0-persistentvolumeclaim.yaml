apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: agent-claim0
  name: agent-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
